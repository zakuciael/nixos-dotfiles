---@type LazySpec
return {
    "nvim-neo-tree/neo-tree.nvim",
    opts = {
        follow_current_file = true,
        filesystem = {
            filtered_items = {
                visible = true,
            },
        },
    },
}
